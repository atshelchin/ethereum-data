# Upload allowlist for Workers Static Assets (gitignore syntax):
# ignore everything at the root, then re-include the data directories.
# When adding a new data directory, add a !/<dir>/ line here.
/*
!/assets/
!/chains/
!/chainlogos/
!/erc7730/
!/index/
!/index.html
!/_headers
