[workspace]
resolver = "2"
members = ["core", "server", "worker"]

[workspace.package]
version = "1.0.0"
edition = "2021"
license = "MIT"

# Do not enable lto/strip/codegen-units=1 in the release profile — they make
# worker-build fail at the wasm-bindgen stage with
# "externref table required for catch wrappers".
